# config file version
apiVersion: 1

datasources:
# InfluxDB - trickster - File system
- name: influx-trickster-fs
  type: influxdb
  access: proxy
  orgId: 1
  url: http://trickster:8480/influx2
  password:
  user:
  database: inspectit
  basicAuth:
  basicAuthUser:
  basicAuthPassword:
  withCredentials:
  isDefault:
  secureJsonData:
    tlsCACert: "..."
    tlsClientCert: "..."
    tlsClientKey: "..."
  version: 1
  editable: false
